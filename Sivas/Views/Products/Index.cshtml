@model Sivas.Models.Products

@{
    ViewBag.Title = "Index";
}

@{
    foreach (Sivas.Models.ProductCategory val in Enum.GetValues(typeof(Sivas.Models.ProductCategory)))
    {
        @Html.ActionLink(val.ToString(), "Category", new { category = val.ToString() });
        <br />
    }
}